<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User</title>
</head>
<body>

    <% if(id) { %>
      Details of <%= users[id].name %>
    <%} else { %>
        <h2>User List:</h2>
        <% users.forEach(user => { %>
            <p>- <%=user.name%> </p>
        <% })  %>
    <% } %>

</body>
</html>